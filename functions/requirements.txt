# Firebase Functions for Python — Holoflow Studio chambers backend
# Keep lean; every dep ships in every deploy.

# Runtime — always
firebase-functions>=0.4.0

# Lithophane chamber
numpy>=1.26
opencv-python-headless>=4.9
scikit-image>=0.22
numpy-stl>=3.1

# pixeldetector chamber (image native-resolution finder)
Pillow>=10.0

# rembg chamber (background removal — downloads U²-Net ONNX on first call)
rembg>=2.0.50
onnxruntime>=1.17

# image-probe chamber (EXIF + dimensions)
# Pillow already above; ExifRead for richer EXIF support
ExifRead>=3.0

# image_resize chamber — AVIF output via Pillow plugin
pillow-avif-plugin>=1.4
