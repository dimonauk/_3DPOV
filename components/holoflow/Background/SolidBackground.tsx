/**
 * SolidBackground — the original Holoflow background.
 *
 * Renders nothing of its own; the global body bg from globals.css
 * (warm-black-950) is what shows through. Kept as an explicit
 * component so it's symmetric with the dynamic variants.
 */

export function SolidBackground() {
  return null;
}
